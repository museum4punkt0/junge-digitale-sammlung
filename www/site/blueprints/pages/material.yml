title: Material Seite
icon: page

options:
  sitemap: hide

status:
  extends: fields/statestwo

tabs:
  # gallery tab
  gallery:
    extends: tabs/filesTab
  # content tab
  content:
    label: Content
    icon: layers
    sections:
      content:
        type: fields
        fields:
          contentlabel:
            type: headline
            label: Content
          layout:
            extends: fields/layout
